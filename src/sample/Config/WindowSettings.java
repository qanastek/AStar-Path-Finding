package sample.Config;

public class WindowSettings {

    // Width resolution of the main window
    public static final int WIDTH = 900 + 200;

    // Height resolution of the main window
    public static final int HEIGHT = 900;
}
