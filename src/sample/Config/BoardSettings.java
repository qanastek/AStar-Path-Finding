package sample.Config;

public class BoardSettings {

    // Matrix dimensions
    public static final int DIMENSIONS = 10;

    // Tile width
    public static final int TILE_WIDTH = WindowSettings.WIDTH / BoardSettings.DIMENSIONS;

    // Tile height
    public static final int TILE_HEIGHT = WindowSettings.HEIGHT / BoardSettings.DIMENSIONS;

    // The stroke width
    public static final int STROKE_WIDTH = 2;
}
